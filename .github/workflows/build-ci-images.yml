name: Build CI images

on:
  push:
    branches: ["main"]
    paths:
      - ".github/images/**"
  # Allow manual rebuild when base images update.
  workflow_dispatch:

jobs:
  opensuse-tumbleweed-ci:
    name: opensuse-tumbleweed-ci
    runs-on: ubuntu-latest
    permissions:
      contents: read
      packages: write

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Log in to GHCR
        uses: docker/login-action@v3
        with:
          registry: ghcr.io
          username: ${{ github.actor }}
          password: ${{ secrets.GITHUB_TOKEN }}

      - name: Build and push
        uses: docker/build-push-action@v5
        with:
          context: .github/images/opensuse-tumbleweed-ci
          push: true
          tags: |
            ghcr.io/${{ github.repository_owner }}/opensuse-tumbleweed-ci:latest
            ghcr.io/${{ github.repository_owner }}/opensuse-tumbleweed-ci:${{ github.sha }}
          labels: |
            org.opencontainers.image.source=${{ github.server_url }}/${{ github.repository }}
            org.opencontainers.image.description=openSUSE Tumbleweed with tar+git+sh for GitHub Actions
